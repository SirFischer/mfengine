#pragma once

#include <SFML/Graphics.hpp>
#include <SFML/OpenGL.hpp>

class window
{
private:
	/* data */
public:
	window(/* args */);
	~window();
};

window::window(/* args */)
{
}

window::~window()
{
}
